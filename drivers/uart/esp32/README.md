# ESP32 UART Driver

Status: scaffolding only.

This directory will contain the ESP32 UART implementation and its minimal executable example.
The target reuses startup support from `platforms/esp32/startup/`.

Build from the repository root:

```bash
make -C drivers/uart/esp32
```
