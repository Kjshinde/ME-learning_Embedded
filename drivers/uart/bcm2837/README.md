# BCM2837 UART Driver

Status: scaffolding only.

This directory will contain the BCM2837 PL011 UART implementation and its minimal executable example.
The target reuses startup support from `platforms/bcm2837/startup/`.

Build from the repository root:

```bash
make -C drivers/uart/bcm2837
```
